<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.userMapper">
    <resultMap id="userMap" type="java.util.Map"/>
    <insert id="userInsert" parameterType="com.example.vet.model.User">
        INSERT INTO USER_TEST (USER_NM, USER_USERNAME, USER_EMAIL, USER_PW, USER_PNUMBER, USER_ADDRESS, COMMON_CODE_PK, USER_BDATE, USER_PROVIDER, USER_PROVIDERID)
        VALUES (#{USER_NM}, #{USER_USERNAME}, #{USER_EMAIL}, #{USER_PW}, #{USER_PNUMBER}, #{USER_ADDRESS}, #{COMMON_CODE_PK}, to_char(sysdate,'YYYY-MM-DD'), #{USER_PROVIDER}, #{USER_PROVIDERID})
    </insert>
    <select id="login" parameterType="string" resultType="com.example.vet.model.User">
        SELECT
            u.USER_PK,
            u.USER_NM,
            u.USER_USERNAME,
            u.USER_EMAIL,
            u.USER_PNUMBER,
            u.USER_ADDRESS,
            u.COMMON_CODE_PK,
            u.USER_BDATE,
            u.USER_PROVIDER,
            u.USER_PROVIDERID,
            c.COMMON_NAME AS ROLE_NAME
        FROM
            USER_TEST u
                JOIN
            COMMON_CODE_TEST c ON u.COMMON_CODE_PK = c.COMMON_CODE_PK
        WHERE
            u.USER_NM = #{userName}
    </select>
</mapper>
